[project]
name = "big-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mrjob>=0.7.4",
    "setuptools>=82.0.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
